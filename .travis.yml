language: cpp
os:
  - linux
  - osx
compiler:
  - gcc
  - clang
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
      - llvm-toolchain-precise-3.7
    packages:
      - libreadline-dev
      - libncurses5-dev
      - g++-4.8
script:
  - tools/travis_${TRAVIS_OS_NAME}.sh
